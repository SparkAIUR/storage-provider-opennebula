# Oracle review reconciliation for v0.5.29

This worktree record reconciles the completed pre-follow-up Oracle report at `/tmp/csi-v0529/oracle-review-result.md`. The external report remains unchanged under the review phase's workspace boundary.

| Oracle concern | Final source correction | Behavior evidence |
| --- | --- | --- |
| Confirmation can erase an active episode before destructive detach | Remove automatic metadata-attached detach; require typed attachment absence for direct attach. Capture the node observation before device inspection and compare its episode during confirmation and deletion retries. | Attachment-absence cases, confirmation during attachment, superseded confirmation and deletion conflicts. |
| Missing-session reconstruction abandons absent-stage or old-superblock binds | Discover using adjacent kubelet ownership before either entry point saves a missing session. Validate all discovered mounts before recovery. | NodeStage and NodePublish cases with two siblings, absent/new stages, read-only flags, unrelated-volume isolation and ambiguous/missing metadata. |
| Failed confirmation deletion suppresses future missing-device episodes | Retain the successful-confirmation reset, but preserve wrong-identity/manual-repair reports during ordinary misses. | Failed deletion followed by a fresh episode; preserved wrong-identity evidence and budgets. |
| Blocked snapshot I/O stalls unrelated hotplug work | Independent per-node workers, brief shared bookkeeping locks, bounded API requests and retries. | Block node A's snapshot write while node B dispatches and node A's queued caller cancels. |
| Timeout report and repair-marker writes can disagree | Remove the second write. Derive runtime repair from the durable terminal report with its episode token; reject standalone runtime marks. | Failed terminal commit retries, late confirmation clears the derived repair, stale timeout cannot recreate it, unrelated newer repair is preserved. |
| Failed completion clearing has no retry or freshness enforcement | Keep pending latest state, schedule bounded retries, and reject superseded versions before accepting or caching persistence. | Two API failures recover without another producer; delayed active snapshot cannot replace completed state. |
| History field merging appears correct, but cache-wide I/O is still coupled | Preserve durable conflict-checked merging while serializing writes per volume outside the cache lock. | Independent writers and conflict tests retained; blocked writer permits unrelated cached reads. |
| Statfs wording exceeds the implementation contract | Keep syscall errors fatal and describe only statfs byte values as diagnostic. | Existing expansion error and geometry regressions retained. |

The release follow-up also resolves fresh repair-state guards, current bounded manual-reprobe authorization, and benchmark admission after restart. See [implementation facts and validation ownership](v0.5.29-review-followup.md).

This reconciliation is a source review and focused regression record. It does not constitute a new Oracle consultation, native hplmon proof, full repository test/lint results, CI completion, release publication or cluster rollout.
